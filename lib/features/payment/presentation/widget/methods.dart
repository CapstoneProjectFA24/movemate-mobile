// methods.dart
// Đây là nơi đặt các method dùng chung, ví dụ như gọi API, xử lý logic.

import 'package:hooks_riverpod/hooks_riverpod.dart';

void handlePayment(WidgetRef ref) {
  // Xử lý thanh toán, có thể gọi API tại đây
  // Ví dụ:
  // final paymentProvider = ref.read(paymentNotifierProvider.notifier);
  // paymentProvider.processPayment();
}
